pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Youself The last Change'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
